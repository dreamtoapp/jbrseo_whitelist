# Helpers

This directory contains utility functions and hooks.

- Utility functions go in `helpers/`
- Custom React hooks go in `helpers/`
- Shared utilities like database clients (Prisma) and helper functions (cn, etc.) are placed here





