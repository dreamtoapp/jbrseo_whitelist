# Actions

This directory contains server actions and business logic.

- Server actions go in `actions/`
- Business logic functions should be organized in subdirectories as needed
- Server actions are server-side functions that can be called directly from client components





