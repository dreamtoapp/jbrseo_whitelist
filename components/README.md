# Components

This directory contains all UI components for the application.

- UI components go in `components/`
- Reusable components should be placed in subdirectories as needed
- shadcn/ui components are installed in `components/ui/`




